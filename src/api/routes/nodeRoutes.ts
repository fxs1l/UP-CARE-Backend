// import express from "express";
// import { createNode, getNodes, updateNode, deleteNode } from "@/api/controllers/nodeController";
// import { nodeEndpoint } from "@/constants/url";

// const router = express.Router();

// router.post(nodeEndpoint, createNode);
// router.get(nodeEndpoint, getNodes);
// router.put(`${nodeEndpoint}/:id`, updateNode);
// router.patch(`${nodeEndpoint}/:id`, updateNode);
// router.delete(`${nodeEndpoint}/:id`, deleteNode);

// export default router;
